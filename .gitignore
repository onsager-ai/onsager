/target
.env
/node_modules
**/node_modules
.DS_Store
*.swp
/.idea
/.vscode/settings.json
/.claude/worktrees
**/dist

# Codegraph local code-index; per-machine dev state.
/.codegraph/

# Local host-port override for host-run cargo dev (machine-specific).
docker-compose.override.yml
compose.override.yml

# Legacy v1 tree (ADR 0029): kept for porting, never built. Its own
# target/node_modules must not be re-added by stray local builds.
/legacy/target
/legacy/**/node_modules
