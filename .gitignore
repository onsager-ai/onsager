/target
.env
/node_modules
**/node_modules
.DS_Store
*.swp
/.idea
/.vscode/settings.json
/.claude/worktrees
**/dist

# Codegraph local code-index; per-machine dev state.
/.codegraph/

# Local host-port override for host-run cargo dev (machine-specific).
docker-compose.override.yml
compose.override.yml

# Duhem run evidence — ignore the DB, commit the .duhem/ VDs
.duhem/*.db
.duhem/*.db-*
